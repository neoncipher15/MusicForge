import React, { useState } from 'react'
import { Heart, Coffee, Star, Gift, CreditCard, CheckCircle, Mail, MessageSquare } from 'lucide-react'

const DonationPage: React.FC = () => {
  const [selectedAmount, setSelectedAmount] = useState<number | null>(5)
  const [customAmount, setCustomAmount] = useState('')
  const [showThankYou, setShowThankYou] = useState(false)
  const [activeTab, setActiveTab] = useState<'support' | 'feedback'>('support')

  const presetAmounts = [3, 5, 10, 20, 50]

  const handleDonate = () => {
    const amount = customAmount ? parseFloat(customAmount) : selectedAmount
    if (amount && amount > 0) {
      setShowThankYou(true)
      setTimeout(() => setShowThankYou(false), 5000)
    }
  }

  const features = [
    { icon: '🎯', title: 'Focus Timer', description: '精准的番茄钟计时器，帮助您保持专注' },
    { icon: '📊', title: '数据分析', description: '详细的使用统计和进度追踪' },
    { icon: '📝', title: '任务管理', description: '轻松创建和管理您的待办事项' },
    { icon: '📅', title: '周计划', description: '规划您的一周，提高工作效率' },
    { icon: '🎨', title: '精美界面', description: '现代化设计，护眼深色主题' },
    { icon: '📱', title: '响应式设计', description: '完美适配各种设备尺寸' }
  ]

  const sponsors = [
    { name: 'Alex M.', amount: 50, message: 'Great app! Keep it up! 🌟' },
    { name: 'Sarah K.', amount: 25, message: 'Love the dark theme! 👏' },
    { name: 'Mike R.', amount: 100, message: 'Best pomodoro app I\'ve used! 💪' },
    { name: 'Emma L.', amount: 10, message: 'Simple and effective! ❤️' }
  ]

  return (
    <div className="animate-fade-in">
      <div className="text-center mb-8">
        <h1 className="text-3xl font-bold text-white mb-4">Support Development</h1>
        <p className="text-gray-400 max-w-2xl mx-auto">
          If you enjoy using our Pomodoro app, consider supporting the development! 
          Your support helps us keep the app free and continuously improving.
        </p>
      </div>

      {/* Tab Navigation */}
      <div className="flex justify-center mb-8">
        <div className="flex gap-2 p-1 bg-gray-800/50 rounded-xl">
          <button
            onClick={() => setActiveTab('support')}
            className={`px-6 py-3 rounded-lg font-medium transition-all duration-300 ${
              activeTab === 'support'
                ? 'bg-purple-500/20 text-purple-400 border border-purple-500/30'
                : 'text-gray-400 hover:text-white'
            }`}
          >
            <Heart size={18} className="inline mr-2" />
            Donate
          </button>
          <button
            onClick={() => setActiveTab('feedback')}
            className={`px-6 py-3 rounded-lg font-medium transition-all duration-300 ${
              activeTab === 'feedback'
                ? 'bg-purple-500/20 text-purple-400 border border-purple-500/30'
                : 'text-gray-400 hover:text-white'
            }`}
          >
            <MessageSquare size={18} className="inline mr-2" />
            Feedback
          </button>
        </div>
      </div>

      {activeTab === 'support' ? (
        <>
          {/* Thank You Message */}
          {showThankYou && (
            <div className="mb-8 p-6 rounded-2xl bg-green-500/20 border border-green-500/30 text-center animate-fade-in">
              <CheckCircle size={48} className="mx-auto text-green-400 mb-3" />
              <h3 className="text-xl font-semibold text-white mb-2">Thank You for Your Support! 🙏</h3>
              <p className="text-gray-300">Your contribution means a lot to us and helps keep this project alive.</p>
            </div>
          )}

          {/* Donation Amount Selection */}
          <div className="max-w-2xl mx-auto mb-8">
            <div className="p-6 rounded-2xl bg-gray-800/50 border border-gray-700/50">
              <h2 className="text-xl font-semibold text-white mb-6 text-center">Choose an Amount</h2>
              <div className="grid grid-cols-5 gap-3 mb-6">
                {presetAmounts.map((amount) => (
                  <button
                    key={amount}
                    onClick={() => { setSelectedAmount(amount); setCustomAmount('') }}
                    className={`py-4 rounded-xl font-semibold transition-all duration-300 ${
                      selectedAmount === amount && !customAmount
                        ? 'bg-purple-500/20 text-purple-400 border border-purple-500/30 scale-105'
                        : 'bg-gray-700/50 text-gray-300 hover:bg-gray-700'
                    }`}
                  >
                    ${amount}
                  </button>
                ))}
              </div>
              <div className="relative mb-6">
                <span className="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400">$</span>
                <input
                  type="number"
                  placeholder="Custom amount"
                  value={customAmount}
                  onChange={(e) => { setCustomAmount(e.target.value); setSelectedAmount(null) }}
                  className="w-full pl-8 pr-4 py-4 bg-gray-700/50 border border-gray-600 rounded-xl text-white placeholder-gray-500 focus:outline-none focus:border-purple-500 text-center text-lg"
                />
              </div>
              <button
                onClick={handleDonate}
                disabled={!customAmount && !selectedAmount}
                className="w-full py-4 bg-gradient-to-r from-purple-500 to-pink-500 text-white font-semibold rounded-xl hover:from-purple-600 hover:to-pink-600 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2"
              >
                <Heart size={20} />
                Support with ${customAmount || selectedAmount || 0}
              </button>
            </div>
          </div>

          {/* Payment Methods */}
          <div className="max-w-2xl mx-auto mb-8">
            <div className="p-6 rounded-2xl bg-gray-800/50 border border-gray-700/50">
              <h3 className="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <CreditCard size={20} className="text-purple-400" />
                Payment Methods
              </h3>
              <div className="grid grid-cols-3 gap-4">
                {['PayPal', 'Ko-fi', 'Buy Me Coffee'].map((method) => (
                  <button
                    key={method}
                    className="p-4 bg-gray-700/50 border border-gray-600 rounded-xl hover:border-purple-500/50 transition-all duration-300"
                  >
                    <Coffee size={32} className="mx-auto mb-2 text-purple-400" />
                    <p className="text-sm text-white">{method}</p>
                  </button>
                ))}
              </div>
            </div>
          </div>

          {/* Sponsors Section */}
          <div className="max-w-2xl mx-auto">
            <div className="p-6 rounded-2xl bg-gray-800/50 border border-gray-700/50">
              <h3 className="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <Star size={20} className="text-yellow-400" />
                Recent Supporters
              </h3>
              <div className="space-y-3">
                {sponsors.map((sponsor, index) => (
                  <div key={index} className="p-4 bg-gray-700/30 rounded-xl border border-gray-600/30">
                    <div className="flex items-center justify-between mb-2">
                      <span className="font-medium text-white">{sponsor.name}</span>
                      <span className="text-purple-400 font-semibold">${sponsor.amount}</span>
                    </div>
                    <p className="text-sm text-gray-400">{sponsor.message}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </>
      ) : (
        <div className="max-w-2xl mx-auto">
          <div className="p-8 rounded-2xl bg-gray-800/50 border border-gray-700/50">
            <h2 className="text-2xl font-bold text-white mb-6 text-center">Share Your Feedback</h2>
            <div className="space-y-6">
              <div>
                <label className="block text-sm text-gray-400 mb-2">Your Name</label>
                <input
                  type="text"
                  placeholder="John Doe"
                  className="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-xl text-white placeholder-gray-500 focus:outline-none focus:border-purple-500"
                />
              </div>
              <div>
                <label className="block text-sm text-gray-400 mb-2">Email</label>
                <input
                  type="email"
                  placeholder="john@example.com"
                  className="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-xl text-white placeholder-gray-500 focus:outline-none focus:border-purple-500"
                />
              </div>
              <div>
                <label className="block text-sm text-gray-400 mb-2">Your Message</label>
                <textarea
                  placeholder="Tell us what you think about the app..."
                  rows={5}
                  className="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-xl text-white placeholder-gray-500 focus:outline-none focus:border-purple-500 resize-none"
                />
              </div>
              <button className="w-full py-4 bg-purple-500/20 text-purple-400 rounded-xl border border-purple-500/30 hover:bg-purple-500/30 transition-all duration-300 flex items-center justify-center gap-2">
                <Mail size={20} />
                Send Feedback
              </button>
            </div>
          </div>
        </div>
      )}

      {/* App Features */}
      <div className="mt-12">
        <h2 className="text-2xl font-bold text-white text-center mb-8">What You Support</h2>
        <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
          {features.map((feature, index) => (
            <div
              key={index}
              className="p-6 rounded-2xl bg-gray-800/50 border border-gray-700/50 hover:border-gray-600 transition-all duration-300 hover:scale-105"
            >
              <span className="text-4xl mb-4 block">{feature.icon}</span>
              <h3 className="text-lg font-semibold text-white mb-2">{feature.title}</h3>
              <p className="text-sm text-gray-400">{feature.description}</p>
            </div>
          ))}
        </div>
      </div>

      {/* Footer Message */}
      <div className="mt-12 text-center">
        <div className="inline-flex items-center gap-3 px-6 py-4 bg-gradient-to-r from-purple-500/10 to-pink-500/10 rounded-2xl border border-purple-500/20">
          <Gift size={24} className="text-purple-400" />
          <p className="text-gray-300">
            Made with <span className="text-red-400">❤</span> for productivity enthusiasts worldwide
          </p>
        </div>
      </div>
    </div>
  )
}

export default DonationPage

